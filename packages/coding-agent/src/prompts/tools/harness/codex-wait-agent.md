Wait for a mailbox update from any live agent: the first incoming message or the first background job to settle, whichever comes first. The wait also ends early when new user input is steered into the active turn. Returns the message content or the settled job's result directly; with nothing pending it returns immediately. `timeout_ms` is accepted but not applied. Re-issue to keep waiting.
