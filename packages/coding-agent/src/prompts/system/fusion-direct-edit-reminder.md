{{! Reminder adapted for OMP from MIT-licensed sources (opencode-fusion).
    See THIRD-PARTY-NOTICES.txt. }}
<system-reminder>
Use the sidekick for routine implementation and running test/build commands. Direct edits are appropriate for a change you can finish and verify within one or two turns, an urgent user-blocking action, or correctness-critical analysis whose implementation and checking you must own.{{#if browserEnabled}} Keep responsibility for inspecting and judging the rendered browser result.{{/if}} Review the worker's evidence yourself; rerun checks only when the evidence is missing, unreliable, or requires your access.
</system-reminder>
